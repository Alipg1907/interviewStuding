package InterviewQuestions;

public class ScenarioOutline {
    /*
     used to execute scenarios multiple times using a different
     set of data. Multiple sets of test data are provided by using
     'Examples' in a tabular structure separated by pipes( || ) .
     We can achieve data driven testing using scenario Outline.
Examples:
      | username | password    | Message                  |
      | Admin12    | Hum@nhrm    | Invalid credentials      |
      | Ad       | Hum@nhrm123 | Invalid credentials      |
      |          |             | Username cannot be empty |


Scenario outlines
are used to run the same steps with different data sets.

    */
}

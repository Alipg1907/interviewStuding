package InterviewQuestions;

public class Background {
    /*Background is in Cucumber testing tool used to define
     a step  or series of steps which are common to all the
     tests/ scenarios in the feature file.
    Background is like scenario containing a number of steps
    but it runs before each
    and every scenario in the feature it is defined.
    */
}
